#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: multiplies with @b
 * @b: multiplies with @a
 * Return: multiplication of @a and @b
 */
int mul(int a, int b)
{
int result;
result = a * b;
return (result);
}
